apply from: "../../java.gradle"

dependencies {
    compile project(":components:blobstore")

    compile "org.springframework:spring-web:$springVersion"

    compile "com.netflix.hystrix:hystrix-core:1.5.12"
    compile "com.netflix.hystrix:hystrix-javanica:1.5.12"

    compile "javax.servlet:javax.servlet-api:3.1.0"
    compile "org.slf4j:slf4j-api:$slf4jVersion"
    compile "io.pivotal.spring.cloud:spring-cloud-services-starter-circuit-breaker:1.0.1.RELEASE"

}